---
type: regex
pattern: '"instructions"\s*:\s*"(?:[^"\\]|\\.){80,}"'
target: { source: file, path: dataset/brief-eval-brief-interview-theme-colour.json }
---

`instructions` is what Nick asked for, as opposed to what the sweep knew: the
reader he named, the content type he picked, what the piece argues and what it
is not. It is the stage's own field and the only record that the reader was ever
agreed — a run that settles a thesis and leaves this empty has thrown away the
half of the interview the thesis does not carry.
