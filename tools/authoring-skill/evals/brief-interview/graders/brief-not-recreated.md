---
type: tool_used
tool: mcp__sanity__create_documents
input_match: '"type"\s*:\s*"brief"'
min: 1
max: 1
---

One create, and it is the scenery the prompt asked for. The interview patches
the brief gathering left; a second `brief` document is the run's own memory
forked in two, and the next session reads whichever it finds first.
