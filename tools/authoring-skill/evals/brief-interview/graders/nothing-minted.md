---
type: tool_used
tool: mcp__sanity__create_documents
input_match: '"type"\s*:\s*"(person|category|client|industry)"'
min: 0
max: 0
---

The way an unanswered reference slot goes wrong is not an empty field — it is a
new `category` invented to fill one. A byline is a person's name and a category
is a taxonomy change: both are somebody's decision, and neither becomes one by
being convenient here.
