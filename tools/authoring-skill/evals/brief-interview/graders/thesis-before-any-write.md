---
type: regex
pattern: '^(?:(?!"name"\s*:\s*"mcp__[sS]anity__patch_documents")[\s\S])*?THESIS AGREED'
target: trace
---

The stage's first write is authorised by the human, not by the interview going
well. Nothing reaches the brief until the agreed sentence has been said back and
nodded at, so the trace shows `THESIS AGREED` with no patch anywhere before it.

Written as one ordering assertion rather than a `tool_order` grader because the
confirmation is a sentence, not a tool call — and it is the sentence that makes
the write legitimate.
