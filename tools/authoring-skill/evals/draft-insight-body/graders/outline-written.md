---
type: regex
pattern: '"outline"\s*:\s*"(?:[^"\\]|\\.){400,}"'
target: { source: file, path: dataset/brief-eval-draft-insight-body-design-system-v2.json }
---

Nick confirmed the shape, so the confirmed shape is in the document. Four
hundred characters is a section list with its annotations; anything shorter is a
sentence about an outline that lives only in the transcript, and the transcript
is what the brief exists to outlive.
