---
type: regex
pattern: '"surface"\s*:\s*"ink"'
match: 'count:2'
target: { source: file, path: piece.json }
---

The dark hero and the dark close, and nothing else on ink. Two is the fixture's
own count; a third ink band is the page claiming three centrepieces.
