---
type: tool_used
tool: mcp__sanity__get_schema
min: 5
---

The page type and each of its four bands, fetched before they are built. The
no-type overview omits field descriptions, and the descriptions are where a
block's authoring guidance lives — a band composed from memory is a band
composed against a schema the run invented.
