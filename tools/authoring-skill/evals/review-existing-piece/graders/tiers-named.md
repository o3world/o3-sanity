---
type: regex
pattern: 'advisory'
flags: mi
match: contains
target: last_message
---

Three tiers, named for what the human does with them — `error`, `craft`,
`advisory`. The advisory tier is the one that disappears first: a review that
grades everything as wrong turns a shape suggestion into an edit nobody asked
for.
