---
type: tool_used
tool: Read
input_match: 'references'
min: 3
---

The references came off the plugin's own disk in this run, not from the model's
memory of a previous one. Three reads, one per file, each naming the
`references/` directory in its path.
