---
type: regex
pattern: '^GATHER GATE: found \d+ / missing \d+ / unverifiable \d+'
flags: m
target: last_message
---

The gate is a line a machine can read or it is a paragraph the next stage may
talk itself past. Three counts, in the fixed order, on their own line — on a
sweep that found almost nothing as much as on one that found plenty.
