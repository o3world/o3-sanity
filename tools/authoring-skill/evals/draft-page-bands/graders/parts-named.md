---
type: regex
pattern: '\([a-zA-Z]+Section[^)\\]*\) [^\\]+, [^\\]+'
target: { source: file, path: dataset/brief-eval-draft-page-bands-design-systems-practice.json }
---

The three engagement shapes are the page's spine, so at least one band repeats.
A repeating band names its parts after the label — "(card list) card 1, card 2"
fidelity — because the count and the order are structure, and structure a
converter has to infer from paragraph breaks is structure it gets wrong.
