---
type: tool_used
tool: mcp__sanity__create_documents
input_match: '"type"\s*:\s*"brief"'
min: 1
max: 1
---

Gathering leaves the brief behind. The subject has no brief in the dataset, so
the stage creates one — once. A second create is a second brief on one subject,
which is the collision this stage exists to catch.
