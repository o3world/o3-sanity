---
type: regex
pattern: '"gaps"\s*:\s*\[\s*"(?:[^"\\]|\\.){10,}"'
target: { source: file, path: dataset/brief-eval-gather-gate-answer-engines.json }
---

Nick refused two numbers outright — the referral mix and anything quantitative —
so this run has gaps whatever else it found. The summary scrolls away and the
field is queryable, which is why the gap list is written to the document and not
only spoken at the gate.
