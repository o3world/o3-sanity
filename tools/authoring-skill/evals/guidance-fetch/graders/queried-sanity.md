---
type: tool_used
tool: mcp__sanity__query_documents
input_match: 'guidance'
min: 1
---

The guidance came from the dataset in this run, not from the model's memory of
a previous one. Tool names are matched case-insensitively, so the plugin's
`sanity` MCP server and this repo's `Sanity` both satisfy it.
