---
type: regex
pattern: '^TYPESET: \S*insight-[a-z0-9-]+ — development, \d+ gaps?.*$'
flags: m
---

The run closes on the document it made, the dataset it made it in, and how much
is still open — matchable, so the hand-off is a state a machine can read rather
than a paragraph a human has to.
