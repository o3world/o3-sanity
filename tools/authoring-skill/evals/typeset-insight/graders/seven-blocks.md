---
type: regex
pattern: '"_type"\s*:\s*"block"'
match: 'count:7'
target: { source: file, path: piece.json }
---

Four paragraphs and three headings — the draft's own count, with the pull quote
and the figure sitting outside it as inline objects. A block more is a paragraph
nobody wrote; a block fewer is a passage that did not arrive.
