---
type: regex
pattern: '^Arc: .+'
flags: m
target: last_message
---

The arc is proposed **by name**, on its own line, on a second pass exactly as on
a first. A shape described in a paragraph is a shape the next draft cannot be
held to — and on a re-run it is also a shape nobody can hold against the
previous one.
