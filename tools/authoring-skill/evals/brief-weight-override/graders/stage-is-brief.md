---
type: regex
pattern: '"stage"\s*:\s*"brief"'
target: { source: file, path: dataset/brief-eval-brief-weight-override-sanity-partner.json }
---

Two stages ran, so the brief says so. `stage` is where a resuming session starts
and the only field that says how far this piece got.
