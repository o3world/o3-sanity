---
type: regex
pattern: '^TYPESET: refused — .+$'
flags: m
target: { source: file, path: refusal.md }
---

The gate is mechanical or it is not a gate. A failing verdict stops stage 5, and
the stop is a line the next thing in the chain can match — a paragraph of
concern reads to a machine exactly like a paragraph of approval.
