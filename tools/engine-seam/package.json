{
  "name": "@o3/engine-seam",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "The engine-purity seam test — data/roster.json enforced over the repo's import graph (#287).",
  "scripts": {
    "format": "prettier --write .",
    "lint": "eslint src/",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@o3/typescript-config": "workspace:*",
    "@types/node": "^24.0.0",
    "typescript": "^5.7.0",
    "vitest": "^4.1.10"
  }
}
