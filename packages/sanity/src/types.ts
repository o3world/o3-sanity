/** A member of a page/section content array as the block dispatcher sees it. */
export interface SanityBlock {
  _type: string
  _key: string
}

export type { Surface, PageType, RoutableType } from './constants'
