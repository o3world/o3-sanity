/**
 * PLACEHOLDER — replaced by `pnpm typegen` (sanity schema extract + typegen
 * generate). Committed so the workspace typechecks before the first typegen
 * run; CI's typegen-drift job fails if this file is stale.
 */
export type SanityQueries = Record<string, never>
